// app/utils/react-code-info.ts

// Common props or interface definitions
export interface BaseComponentProps {
  className?: string;
  id?: string;
  style?: React.CSSProperties;
}

// Reusable style objects
export const commonStyleObjects = {
  pageContainer: {
    minHeight: '100vh',
    background: `linear-gradient(rgba(0, 0, 0, 0.8), rgba(0, 0, 0, 0.8)), url('/Lambeau68-1024x690.jpg')`,
    backgroundSize: 'cover',
    backgroundPosition: 'center',
    padding: '2rem'
  },
  
  mainHeading: {
    fontSize: '2.5rem',
    color: '#FFD700',
    textAlign: 'center',
    marginBottom: '2rem',
    textTransform: 'uppercase',
    letterSpacing: '0.1em',
    textShadow: '2px 2px 4px rgba(0, 0, 0, 0.7)'
  }
};

// Helper functions
export function formatTimestamp(timestamp: string): string {
  // Logic to format timestamps consistently
  return timestamp;
}
